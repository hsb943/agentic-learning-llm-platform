apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: learning-frontend-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: learning-frontend
                port:
                  number: 80
