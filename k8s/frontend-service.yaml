apiVersion: v1
kind: Service
metadata:
  name: learning-frontend
  labels:
    app: learning-frontend
spec:
  selector:
    app: learning-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
